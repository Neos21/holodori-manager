# 実装計画チェックリスト

**実行ルール : このファイルの未完チェックリストは上から順に実行すること。AI はファイル内で最初に出現する `- [ ]` (未完了のチェックリスト項目) を次タスクとして扱い、ユーザ確認なしに別タスクへ移行しない。**

このファイルはプロジェクトの実装フェーズとチェックリストを上から順に実行できるように整理したものです。

実装ルールは [AGENTS.md](AGENTS.md) に集約しています。詳細は AGENTS.md を参照してください。

- 目的 : フェーズ順に実装を進め、各タスクの対象ファイルや小さな実装単位が一目で分かるようにする
- 方針 : 各フェーズは上から順に完了させる。フェーズ内で完了した項目には `[x]` を付ける
- このファイルはフェーズ順で進めることを前提に整理している。対象ファイルなどの補足情報は必要に応じて各フェーズの中に追記すること


## 現状サマリ

- バックエンド、フロントエンドともに MVP 実装は済
- フロントエンドの UX を中心に細かな調整を進めていく段階


## タスクリスト (上から順に実装していくこと)

- `board-nodes.tsx` のデザイン・UI 修正 : 全体的に `cards.tsx`・`holomems.tsx` の作りに寄せる
    - [x] データが0件の時に「登録されているマスはありません。」と「新規マス追加」ボタンを表示するようにする。
    - [x] 新規マス追加フォームで「カテゴリ」セレクトボックスを、選択した項目の色に応じて 黄 `warning`・緑 `success`・赤 `error`・青 `info` で色付けする
    - [x] マス編集フォームで更新しようとした時、`boardNodes.patch('/:id'` でエラーが出る。原因は `boardNodeSchema.partial()` 部分で、このスキーマは `superRefine()` を使っているためと思われる。画面から一部項目しかデータを送らない、ということはないと思うので、恐らく `partial()` を外すだけで解消できると思うが、「報酬アップ対象アイテム」で `disabled` を使っているため本当に動くか不明、確認が必要
    - [x] `h2` のカテゴリ名称は `badge` にせず簡素な見出しにする。`h3` のホロメン情報は `border` の色をカテゴリ名の色に合わせる
    - [x] テーブルとホロメンメモはいかなる画面幅でも常に2カラムにする
    - [x] テーブルは、スマホで閲覧する際に1画面に情報量を多く表示したい。`cards.tsx`・`holomems.tsx` のテーブルと同様に余白を小さく調整する。現状は画面幅が狭いと見出し文字列などが見切れるほど列幅が狭くなってしまうのでそれは避けたい。見出しの文字列は突っ張り棒になるように `nowrap` 相当とする。解放状況に応じて、解放されていない行は `bg-base-300` でグレーアウトとし「解放」列自体は設けないようにする。
    - [x] テーブルの「基礎効果量」と「最終レート」は必ず `4.00` のように小数以下第2位まで表示するよう整形する。「コネクト増加率」は小数がないため、「115%」のように `%` を後ろに付けて表示する。「基礎効果量」については「マス編集」フォームダイアログでの初期表示時も小数以下第2位まで表示に揃えたい。
    - [x] ホロメンメモのデザインは左隣のテーブルに近付ける。見出し行の高さをテーブルと揃え、見出しとホロメンメモ本文との間にテーブルと同様の線を入れる。イメージとしては「テーブルの一番右の列であり、行結合してホロメンメモを表示している」ような見栄えにしたい。
    - [x] 画面幅が狭い場合は、テーブル部分だけが横スクロールするのではなく、テーブルとホロメンメモをまとめて横スクロールできるようにする。
    - [x] ホロメンごとにテーブルが別れて表示されるが、テーブルとホロメンメモの各列の幅がズレないようにすること。
    - [x] スタイリングのトンマナが `cards.tsx`・`holomems.tsx` とズレてる。やたらと装飾せず簡素に。`alert` などのスタイリングを統一する。モーダル等の `className` 実装方式を統一する。`flex` や `grid` の多用を避ける。SVG アイコンなどの装飾は不要。
    - [x] `board-nodes.tsx` 内の TODO コメントの内容を修正反映して解消する
- [x] ページ初期表示時、JWT が無効と判断して `/` にリダイレクトした際に、「再度ログインしてください」のメッセージを `alert alert-warning alert-soft` で表示する
- [x] ログイン後のサイドメニューに「ログアウト」ボタンを配置する。`useAdminStore` の `logout()` を実行して LocalStorage をクリアし `/` に `navigate()` 遷移する
- [x] サイドメニューに `memo` の編集欄を設ける : UI 仕様は README に記載あり
- [ ] GET `/api/holomems` を複数画面で呼んでいるので、Store 管理にして API コール回数削減、パフォーマンス向上を狙いたい

### 優先度低

- [ ] サーバサイドの `error` レスポンスメッセージで共通化できる文言を `server/constants/` あたりでまとめる
- [ ] README の API 一覧の記載と実装が合っているか確認する
    - [ ] 使っていない API が実装にあれば、用途を再確認したうえで削除する
    - [x] 使っていて README に記載がなければ README を加筆修正する
    - [x] README のパス・HTTP Method を現在の実装と一致させる
- [ ] ホロワーク機能を参考に、他画面・バックエンドの関数・型・主要オブジェクトにドキュメンテーションコメントを拡充する
    - 型から明らかな説明を重複させず、`null` の条件、処理順、ソート順、Map による集約理由などの実装意図を優先する
    - 対象を指す助詞は「～へ」より「～に」を優先し、何に対する説明なのかを正確に表現する
    - 名前と型だけで役割が自明な変数などには、内容を言い換えるだけのコメントを追加しない
    - 関連する型・関数・定数などを参照すると理解しやすい場合は `@see {@link XXX}` を使用する
    - 用語・助詞・句読点などの日本語表現は既存コメントと比較し、同じ概念に同じ表現を使用する
- [ ] README・AGENTS を中心としたドキュメント構成を継続的に整理する
    - README は現行仕様、AGENTS は常時適用するルール、TASKS は実行順、`docs/` は機能固有の設計意図・判断理由・ロードマップを扱う
    - 同じ説明の重複を避け、詳細ドキュメントへのリンクで AI が読む情報量を抑える
