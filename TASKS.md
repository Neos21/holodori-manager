# 実装計画チェックリスト

**実行ルール : このファイルの未完チェックリストは上から順に実行すること。AI はファイル内で最初に出現する `- [ ]` (未完了のチェックリスト項目) を次タスクとして扱い、開発者の確認なしに別タスクに移行しない。**

このファイルはプロジェクトの実装フェーズとチェックリストを上から順に実行できるように整理したものである。

実装ルールは [AGENTS.md](AGENTS.md) を入口として `docs/agent-rules/` に分類している。詳細は `AGENTS.md` が指定する作業対象別のルールを参照すること。

- 目的 : フェーズ順に実装を進め、各タスクの対象ファイルや小さな実装単位が一目で分かるようにする
- 方針 : 各フェーズは上から順に完了させる。フェーズ内で完了した項目には `[x]` を付ける
- このファイルはフェーズ順で進めることを前提に整理している。対象ファイルなどの補足情報は必要に応じて各フェーズの中に追記すること


## タスクリスト

### [ ] ミニゲーム「High & Low」計算ページに関するドキュメント拡充

ホロドリ内に「ハイアンドロー」という「ファイブカードドロー (ドローポーカー、ビデオポーカー)」のミニゲームがある。5枚のトランプで役を揃えるとコインを獲得でき、その報酬を2倍にできる「ダブルアップチャンス」に任意に挑戦できる。ミニゲームのルール・仕様に基づき、獲得コインを最大化するための計算ページを作った (`client/pages/high-and-low-calculator/` 配下)。

「High & Low」のゲームシステムの文書化、および計算ページの設計を文書化する。合わせて、ユニットテスト用途に作られた `sanity-check.ts` について、`client/` 配下に配置せず他所に移動するか削除するかを検討する。

#### ビデオポーカー部分のルール

- ユーザは1プレイにつき50コインをベットする
- 5枚のトランプが配られ、0～5枚のカードをそのまま保持するか否か選べる
    - 山札は52枚 + ジョーカー1枚の53枚
    - ジョーカーはオールマイティカードである
    - A (エース) は K (キング) より大きな最上位の数である。2 が最低の数となる
- 保持しなかったカードは、山札の別のカードと差し替えられる
- 差し替え後の5枚で、以下のいずれかの役を満たすと、倍率に応じたコインがもらえる
    - ワンペア : 同じ数字が2枚の組み合わせが1つ : ×0 (これ単体では意味なし)
    - ツーペア : 同じ数字が2枚の組み合わせが2つ : ×4 (= 200コインになる)
    - スリーカード : 同じ数字が3枚 : ×4
    - ストレート : 5枚のカードが連番で並ぶ : ×8
        - 10・J・Q・K・A : K から A は連番扱いでストレートが成立する
        - J・Q・K・A・2 : A から 2 は連番扱いにならないのでストレートが成立しない
    - フラッシュ : 5枚のカードのマークが同じ : ×14
    - フルハウス : ワンペア + スリーカードの組み合わせ : ×16
    - フォーカード : 同じ数字が4枚 : ×30
    - ストレートフラッシュ : ストレート + フラッシュの組み合わせ : ×60
    - ファイブカード : 同じ数字が4枚 + ジョーカー : ×140
    - ロイヤルフラッシュ : ストレートフラッシュを 10・J・Q・K・A で完成 : ×200
- 役を満たすと、ダブルアップチャンスを行うか辞退するかが選べる

#### ダブルアップチャンス部分のルール

- ダブルアップチャンスでは、ジョーカーなしの52枚の山札 (ビデオポーカー部分とは無関係) からランダムな1枚が提示され、次に出されるカードがそれよりも大きな数のカードか、小さなカードかを予測する
    - こちらも、A (エース) は K (キング) より大きな最上位の数である。2 が最低の数となる
    - 場に出ているカード (例えば「ハートの5」) とスート違いの同値のカード (例えば「スペードの5」) が出た場合は、「たかい」「ひくい」どちらを選択していてもどちらとも判定されず、「スペードの5」に対して改めて「たかい」か「ひくい」かを選び直すことになる
- ダブルアップに成功したら、その場で辞退して確定したコインをもらうか、もう一度ダブルアップチャンスを続けるか選べる
- ダブルアップに失敗すると獲得コインは0枚になる

#### 1プレイ、1日の獲得コイン上限

- 1プレイにおけるダブルアップチャンスに関しては「10000枚超過時点」 (200コインから始まった場合、12800コイン獲得時点) で、ダブルアップチャンス挑戦権がなくなり、強制的にゲーム終了 (12800コイン獲得) で確定する
- 1日あたりの獲得コインの上限は「累計20000コイン超過時点」なので、「12800コイン獲得 × 2プレイ = 25600コイン」獲得すると上限に達し、ビデオポーカー自体がプレイ不可能になる
    - よって、例えば「12800枚→6400枚→12800枚」と3プレイに別けて獲得することで、1日あたり32000コインを獲得することもできる。この「1日に獲得できるコイン数」をなるべく最大化したい
- 「1日あたりこのミニゲームに挑戦できる上限」(プレイ回数そのものの上限) はない

#### 実装途中のファイル群について

- `client/pages/high-and-low-calculator/` 配下に、EV 計算部分のロジックが実装途中で置いてある。最終的には React SPA (ブラウザ上) だけで動作する、サーバサイドを使わずに実装したい
- 全32パターンの厳密 EV 計算に約16.5秒かかり、「捨て4〜5枚」のパターンで特に時間がかかる
    - 捨て枚数 : 組合せ数
    - 0枚 : 1
    - 1枚 : 48
    - 2枚 : 1,128
    - 3枚 : 17,296
    - 4枚 : 194,580 (×5パターン)
    - 5枚 : 1,712,304 (×1パターン)
- 「全カード交換」1パターンだけで170万通りの評価が必要になるのが特にボトルネック。Web Worker に逃がしたりサーバサイドで計算したりしても計算自体には物量がかかる
    - 遅い主因は、JS 側で「カードオブジェクトを生成 → 配列を作る→ Map を使う → 毎回ソートする」といったオーバーヘッドの積み重ねであり、170万通りの計算自体は CPU で1秒程度で終わると予想される
- そこで、組合せ論的なショートカット計算方式を用意しておき、デフォルトでは「ショートカット計算モード」でなるべく高速に動き、ユーザがモード選択することで「厳密 EV 計算モード」でも動かせるようにしたいと思っている
    - 前述のブルートフォースな計算方式からアルゴリズムを変える (全列挙をやめる) 方が費用対効果が高いと考える
- ショートカット方式の設計
    - 「全列挙 vs サンプリング」で悩んだが、ゲームをプレイしていての体感 (3枚捨て・4枚捨てを選ぶ機会が多い) を活かして、ハイブリッド方式にする
    - 組合せ数が少ない (目安2万通り以下 = 捨て0〜3枚) はそのまま厳密計算 (体感でも一瞬で終わる)
    - 組合せ数が多い (捨て4〜5枚) はモンテカルロサンプリング (ランダムに数万回試行して確率を推定・精度を保つため5万回試行としておく) に自動切替
    - `mode: 'exact'` を明示指定すれば、重いケースも含めて全部厳密計算に強制できる
    - これにより16.5秒かかっていた計算が3秒程度まで短縮できそう
- 実装されていないのは以下の内容
    - React UI : カード入力ボタン、保持推奨表示、ダブルアップ確率表示
        - UI 実装がないので、ビジネスロジック部分に無駄な実装があるかも
    - 1日単位のプレイ回数戦略を算出するサービス : 「12800+6400+12800=32000」のように複数プレイに分けて日次上限を最大活用する組み立て方 (整数計画寄りの最適化)
    - ダブルアップの継続判断に「本日の獲得済み上限」を絡めているのは誤り
        - 日次上限は「新しいプレイを開始できるかどうか」だけに関わるルールで、進行中のダブルアップ・チェーンを打ち切る理由にはならない (チェーンを続けても新しいプレイの機会を消費するわけではないので、続けられるなら常に続けた方が有利)。この判断は UI 側で「新規プレイを開始できるか」のチェックに移し、`DoubleUpService` からは削除すべき

#### UI 実装に際して

- ダブルアップチャンスの「挑戦する or しない」の判断は、常に「挑戦する」が確率的に正解
- `recommendAction` は「提示カードの確率」を受け取る想定だったが、実際のゲームフローだと「続ける or 辞退する」の選択は次のカードが見える前に発生する (成功後、次のカードが出る前に選択)
    - なので、渡すべきは「特定のカードの確率」ではなく「残りデッキ全体で有利な方を選び続けた場合の平均成功率」だった
    - これを計算する `calcExpectedBestSideProbability` の追加が必要。既存の `calcProbabilities` を、残りデッキの全カードそれぞれについて呼び出して平均するだけなので、実装はシンプル
- 画面構成の考え方
    - 新規プレイ入力 : 5枚 + 本日の獲得済みコイン数 (編集可)
    - 保持カード推奨 : EV の高い順に32パターン表示
    - 結果入力 : 交換後5枚 → 成立役を判定
        - 不成立 → 新規プレイに戻る
    - 役成立 → ダブルアップ : 提示カード → 確率 → 継続 or 辞退
        - 辞退・上限到達・失敗 → 新規プレイに戻る
- 新規プレイ入力
    - カード入力はスート4つ×ランク13(+ジョーカー)のボタングリッドを横並びに5枠分
        - カード1枚につき、「スート4種のボタン」「ランク13 + ジョーカー1のボタン」を常時表示しておき、全10クリックで5枚の状態を選択できるようにする。モーダル等は使わない
    - 「本日すでに稼いだコイン」は数値入力欄として常時表示し、いつでも上書き修正可能にする (初期値0、プレイを重ねるたびに自動加算されるが、途中から使い始めたときのために手動編集を許可)
- 保持カード推奨
    - `PokerService.evaluateAllHoldOptions()` の結果に基づき、保持推奨は5枚の下に「保持すべき」ラベルを出す
    - 実際にユーザがゲーム内でどう操作したかは UI 側は知らなくてよい
- 結果入力
    - 交換後の5枚のカードについて、結果を「セレクトボックスで役を選択」+「不成立ボタン」を入力させる。`PokerService.evaluateHand()` で役を自動判定 → 倍率とコインを表示
- ダブルアップ
    - 現在の見込みコインと、提示カードを入力すると `DoubleUpService.calcProbabilities()` で確率を出し、`recommendAction()` の推奨 (継続 or 辞退) を表示する
    - ユーザーが実際に選んで結果 (成功 or 失敗) を入力する、を繰り返すループ
    - 以下の3パターンでリセット (新規プレイ入力に戻る) し、その際に確定コインを「本日の獲得済み」に自動加算する :
        - 辞退を選択
        - currentCoins > 10000 になった時点で自動的に強制終了 (ボタンを非活性化 or 自動遷移)
        - 成功 or 失敗の結果入力で「失敗」が入力された (この場合は加算せず 0 のままリセット)


-----


- (現在タスクなし・開発者の指示に沿って個々の作業を実施する)
